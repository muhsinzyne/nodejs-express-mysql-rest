<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Ubuntu:wght@300;400;500&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: 'Ubuntu', sans-serif;
        }

        /* Animation for input labels */
        .floating-label {
            position: relative;
        }

        .floating-label input:focus + label,
        .floating-label input:not(:placeholder-shown) + label {
            transform: translateY(-1.75rem);
            font-size: 0.75rem;
            color: #63B3ED;
        }
    </style>
    <title>Modern Login</title>
</head>
<body class="min-h-screen flex items-center justify-center bg-gray-900">

<div class="w-full h-screen flex">
    <!-- Left side for branded wallpaper -->
    <div class="hidden md:flex w-1/2 bg-cover bg-center" style="background-image: url('https://source.unsplash.com/collection/928423/800x600');">
        <div class="bg-blue-900 bg-opacity-50 flex flex-col items-center justify-center w-full h-full text-white">
            <h1 class="text-4xl font-bold mb-4">Welcome Back!</h1>
            <p class="text-lg max-w-md text-center">Experience a secure and seamless login process tailored just for you. Enjoy the power of our platform at your fingertips.</p>
        </div>
    </div>

    <!-- Right side for the login form -->
    <div class="flex flex-col w-full md:w-1/2 items-center justify-center p-8 bg-blue-800">
        <div class="w-full max-w-md bg-gray-800 text-white rounded-lg shadow-lg p-8">
            <h2 class="text-2xl font-semibold text-center mb-6">Sign In</h2>
            <form method="post" action="/admin/queue-dashboard/login" class="space-y-6">
                
                <!-- Username field with floating label animation -->
                <div class="floating-label">
                    <input type="text" name="username" placeholder=" " required
                        class="w-full bg-gray-700 rounded p-4 text-sm placeholder-transparent focus:outline-none focus:ring-2 focus:ring-blue-400 transition-all duration-300">
                    <label class="absolute top-4 left-4 text-gray-400 text-sm pointer-events-none transition-all duration-200">Username</label>
                </div>

                <!-- Password field with floating label animation -->
                <div class="floating-label">
                    <input type="password" name="password" placeholder=" " required
                        class="w-full bg-gray-700 rounded p-4 text-sm placeholder-transparent focus:outline-none focus:ring-2 focus:ring-blue-400 transition-all duration-300">
                    <label class="absolute top-4 left-4 text-gray-400 text-sm pointer-events-none transition-all duration-200">Password</label>
                </div>

                <button type="submit"
                    class="w-full bg-blue-600 hover:bg-blue-700 transition-all duration-300 py-3 rounded text-white uppercase font-semibold text-sm">
                    Login
                </button>

                <!-- Forgot password link -->
                <p class="text-center text-sm text-gray-400 mt-4">
                    <a href="#" class="hover:text-blue-400 transition-all duration-200">Forgot Password?</a>
                </p>
                
                <% if (message) { %>
                    <p class="text-red-500 text-center text-sm mt-4">Invalid username or password.</p>
                <% } %>
            </form>
        </div>
    </div>
</div>

</body>
</html>
