<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://fonts.googleapis.com/css2?family=Ubuntu:wght@300;400;500&display=swap" rel="stylesheet">
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            font-family: 'Ubuntu', sans-serif;
        }
    </style>
    <title>Landing Page</title>
</head>
<body class="min-h-screen flex items-center justify-center bg-gray-900">
    <div class="w-full h-screen flex">
        <!-- Left side for branded wallpaper -->
        <div class="hidden md:flex w-1/2 bg-cover bg-center" style="background-image: url('https://source.unsplash.com/collection/928423/800x600');">
            <div class="bg-blue-900 bg-opacity-50 flex flex-col items-center justify-center w-full h-full text-white">
                <h1 class="text-4xl font-bold mb-4">We will be back!!!</h1>
                <p class="text-lg max-w-md text-center">GoCoMart never died!! Stay tuned for an exciting comeback. We'll be back stronger than ever.</p>
            </div>
        </div>

        <!-- Right side with message and optional subscription form -->
        <div class="flex flex-col w-full md:w-1/2 items-center justify-center p-8 bg-blue-800">
            <div class="w-full max-w-md bg-gray-800 text-white rounded-lg shadow-lg p-8">
                <h2 class="text-2xl font-semibold text-center mb-6">Stay Connected!</h2>
                <p class="text-center text-lg mb-4">We'll notify you about the latest updates and news. Subscribe to our newsletter to stay informed.</p>
                <form method="POST" action="/subscribe" class="space-y-6">
                    <!-- Email field -->
                    <div class="floating-label">
                        <input type="email" name="email" placeholder=" " required class="w-full bg-gray-700 rounded p-4 text-sm placeholder-transparent focus:outline-none focus:ring-2 focus:ring-blue-400 transition-all duration-300">
                        <label class="absolute top-4 left-4 text-gray-400 text-sm pointer-events-none transition-all duration-200">Email Address</label>
                    </div>
                    <button type="submit" class="w-full bg-blue-600 hover:bg-blue-700 transition-all duration-300 py-3 rounded text-white uppercase font-semibold text-sm">
                        Subscribe
                    </button>
                </form>
            </div>
        </div>
    </div>
</body>
</html>
